/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['photo.hotellook.com'], // Ha Ratehawk képeket akarsz betölteni
  },
}

module.exports = nextConfig
